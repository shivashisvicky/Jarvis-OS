(() => {
  'use strict';
  const PIPED=['https://pipedapi.kavin.rocks','https://pipedapi.tokhmi.xyz','https://pipedapi.moomoo.me','https://pipedapi.syncpundit.io','https://api-piped.mha.fi','https://piped-api.garudalinux.org','https://pipedapi.rivo.lol','https://pipedapi.leptons.xyz'];
  const INVIDIOUS=['https://inv.nadeko.net','https://invidious.nerdvpn.de','https://yt.chocolatemoo53.com'];
  const $=(s,r=document)=>r.querySelector(s); const $$=(s,r=document)=>[...r.querySelectorAll(s)];
  const esc=s=>String(s??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
  const json=async(url,timeout=2800)=>{const c=new AbortController(),t=setTimeout(()=>c.abort(),timeout);try{const r=await fetch(url,{signal:c.signal,cache:'no-store',headers:{Accept:'application/json'}});if(!r.ok)throw new Error(String(r.status));return await r.json()}finally{clearTimeout(t)}};
  const normalize=(v,source)=>{const id=String(v.videoId||v.id||'');if(!/^[A-Za-z0-9_-]{11}$/.test(id))return null;return{id,title:v.title||'Untitled video',author:v.author||v.uploader||v.uploaderName||'YouTube',views:v.viewCountText||v.views||(v.viewCount?`${Number(v.viewCount).toLocaleString()} views`:''),thumb:v.thumbnail||v.thumbnailUrl||v.videoThumbnails?.find(x=>/high|medium|maxres/i.test(x.quality||''))?.url||v.videoThumbnails?.[0]?.url||`https://i.ytimg.com/vi/${id}/hqdefault.jpg`,source}};
  async function search(q){const tasks=[];PIPED.forEach(base=>tasks.push(json(`${base}/search?q=${encodeURIComponent(q)}&filter=videos&region=IN`).then(d=>(Array.isArray(d)?d:d.items||[]).map(v=>normalize(v,'PIPED')).filter(Boolean))));INVIDIOUS.forEach(base=>tasks.push(json(`${base}/api/v1/search?q=${encodeURIComponent(q)}&type=video&region=IN&page=1`).then(d=>(Array.isArray(d)?d:[]).map(v=>normalize(v,'INVIDIOUS')).filter(Boolean))));try{const first=await Promise.any(tasks);return first.slice(0,10)}catch{return[]}}
  const idOf=raw=>{try{const u=new URL(raw);if(u.hostname==='youtu.be')return u.pathname.split('/').filter(Boolean)[0]||null;if(u.hostname.includes('youtube.com'))return u.searchParams.get('v')||u.pathname.split('/').filter(Boolean).pop()||null}catch{}return/^[A-Za-z0-9_-]{11}$/.test(String(raw).trim())?String(raw).trim():null};
  function mount(w){if(!w||w.dataset.jv3media==='1')return;w.dataset.jv3media='1';w.innerHTML=`<section class="panel media-main"><div class="search-bar"><input id="videoQuery" placeholder="Search videos, channels or topics…"><button class="primary" id="videoSearch" type="button">SEARCH</button></div><div class="media-search-links"><button type="button" data-video-provider="trending">TRENDING</button></div><div id="jarvisPlayer" class="jv3-player"><div class="jv3-player-empty"><div>▶<strong>JARVIS VIDEO CORE</strong><small>Search results and playback stay inside this console.</small></div></div></div><div class="request-line"><input id="videoUrl" placeholder="Optional: YouTube URL, video ID or direct MP4 URL"><button class="primary" id="playVideo" type="button">PLAY</button></div></section><aside class="panel media-side"><div class="panel-head"><span>VIDEO INTELLIGENCE</span><span class="live" id="jvcStatus">READY · IN-HOUSE VIDEO SEARCH</span></div><div id="videoResults" class="jv3-video-results"><div class="empty">Enter a topic to search.</div></div></aside>`;
    const input=$('#videoQuery',w),results=$('#videoResults',w),player=$('#jarvisPlayer',w),status=$('#jvcStatus',w);
    const play=item=>{player.innerHTML=`<iframe title="${esc(item.title)}" src="https://www.youtube-nocookie.com/embed/${encodeURIComponent(item.id)}?rel=0&playsinline=1&modestbranding=1" allow="autoplay; encrypted-media; picture-in-picture; fullscreen" allowfullscreen></iframe>`;status.textContent='PLAYING · JARVIS EMBED PLAYER'};
    const draw=items=>{results.innerHTML=items.map(v=>`<button class="jv3-video-card" type="button" data-video-id="${esc(v.id)}"><img class="jv3-video-thumb" loading="lazy" src="${esc(v.thumb)}" alt=""><span><strong>${esc(v.title)}</strong><small>${esc(v.author)}${v.views?` · ${esc(v.views)}`:''}</small></span><b class="jv3-video-play">▶</b></button>`).join('')||'<div class="jv3-fallback">No video index responded. JARVIS will not redirect you. Try again.</div>';$$('.jv3-video-card',results).forEach(b=>b.addEventListener('click',()=>{const item=items.find(x=>x.id===b.dataset.videoId);if(item)play(item)}))};
    const run=async q=>{q=String(q||'').trim();if(!q){status.textContent='READY · ENTER A VIDEO SEARCH';return}status.textContent='SEARCHING · JARVIS VIDEO INDEX';results.innerHTML='<div class="jv3-status">Searching multiple public video indexes…</div>';const items=await search(q);draw(items);status.textContent=items.length?`${items.length} RESULTS · STAYING INSIDE JARVIS`:'VIDEO INDEX DEGRADED · NO REDIRECT'};
    $('#videoSearch',w).onclick=()=>void run(input.value);input.onkeydown=e=>{if(e.key==='Enter'){e.preventDefault();void run(input.value)}};$$('[data-video-provider]',w).forEach(b=>b.onclick=()=>{input.value='trending videos India';void run(input.value)});
    $('#playVideo',w).onclick=()=>{const raw=$('#videoUrl',w).value.trim(),id=idOf(raw);if(id)play({id,title:'Pasted YouTube video'});else if(/^https?:\/\//i.test(raw)){player.innerHTML=`<video controls playsinline src="${esc(raw)}"></video>`;status.textContent='PLAYING · DIRECT MEDIA'}else status.textContent='PASTE A YOUTUBE URL, VIDEO ID OR DIRECT MP4'};
  }
  let timer=0;
  const tick=()=>{const w=$('.media-workspace');if(w&&w.dataset.jv3==='1'&&w.dataset.jv3media!=='1'){clearTimeout(timer);timer=setTimeout(()=>mount(w),350)}};
  const o=new MutationObserver(tick);o.observe(document.documentElement,{childList:true,subtree:true});
  if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',tick,{once:true});else tick();
})();
