(() => {
  'use strict';
  if (window.__JARVIS_IN_SHELL_WEB_V1__) return;
  window.__JARVIS_IN_SHELL_WEB_V1__ = true;
  const $ = s => document.querySelector(s);
  const esc = s => String(s ?? '').replace(/[&<>\"']/g, c => ({'&':'&amp;','<':'&lt;','>':'&gt;','\"':'&quot;',"'":'&#39;'}[c]));
  const state = { generation: 0, cache: new Map() };
  const providers = { brave:q=>`https://search.brave.com/search?q=${encodeURIComponent(q)}`, bing:q=>`https://www.bing.com/search?q=${encodeURIComponent(q)}` };
  const fetchText = async (url, ms=10000) => { const c=new AbortController(),t=setTimeout(()=>c.abort(),ms); try{const r=await fetch(url,{signal:c.signal,cache:'no-store',headers:{Accept:'text/plain,text/html,*/*'}});if(!r.ok)throw Error(`HTTP ${r.status}`);return await r.text();}finally{clearTimeout(t);} };
  const jina=url=>`https://r.jina.ai/http://${url.replace(/^https?:\/\//,'')}`;
  function parseLinks(text,engine){
    const out=[],seen=new Set();
    const add=(title,url,snippet='')=>{try{const u=new URL(url);if(!/^https?:$/.test(u.protocol))return;if(/(^|\.)brave\.com$|(^|\.)bing\.com$/i.test(u.hostname))return;if(seen.has(u.href))return;seen.add(u.href);out.push({title:String(title||u.hostname).trim(),url:u.href,snippet:String(snippet||'').replace(/\s+/g,' ').trim(),source:engine.toUpperCase()});}catch{}};
    const md=/\[([^\]]{2,240})\]\((https?:\/\/[^)\s]+)\)/g;
    for(const m of String(text).matchAll(md)){const before=String(text).slice(Math.max(0,m.index-180),m.index);const after=String(text).slice(m.index+m[0].length,m.index+m[0].length+320);add(m[1],m[2],(before+' '+after).replace(/\[[^\]]+\]\([^)]*\)/g,' '));if(out.length>=20)break;}
    if(!out.length){const doc=new DOMParser().parseFromString(String(text),'text/html');doc.querySelectorAll('a').forEach(a=>{if(out.length>=20)return;const h=a.textContent?.trim()||'';const href=a.getAttribute('href')||'';if(h.length>2)add(h,href,a.parentElement?.textContent||'');});}
    return out.slice(0,20);
  }
  async function search(q,engine){const query=String(q||'').trim();if(!query)return;const token=++state.generation,results=$('#jwsResults'),status=$('#jwsStatus');if(!results||!status)return;status.textContent=`SEARCHING ${engine.toUpperCase()} · ${query}`;results.innerHTML='<div class="empty">JARVIS is searching the live web inside this shell…</div>';try{const key=`${engine}:${query}`;let items=state.cache.get(key);if(!items){const html=await fetchText(jina(providers[engine](query)));items=parseLinks(html,engine);state.cache.set(key,items);}if(token!==state.generation)return;if(!items.length)throw Error('NO_RESULTS');results.innerHTML=items.map(x=>`<article class="jws-card"><div class="jws-source">${esc(x.source)}</div><h3><a href="${esc(x.url)}" target="_blank" rel="noopener noreferrer">${esc(x.title)}</a></h3><p>${esc(x.snippet||'Live web result')}</p><small>${esc(new URL(x.url).hostname)}</small></article>`).join('');status.textContent=`${items.length} LIVE RESULTS · ${engine.toUpperCase()}`;}catch(e){if(token!==state.generation)return;results.innerHTML='<div class="empty">Live in-shell search is temporarily unavailable. JARVIS did not redirect your tab.</div>';status.textContent=`DEGRADED · ${String(e?.message||'SEARCH FAILED')}`;}}
  function mount(){const input=$('#webQuery'),provider=$('#webProvider'),button=$('#webSearch');if(!input||!provider||!button)return;if(!$('#jwsStatus')){const box=document.createElement('div');box.id='jwsStatus';box.className='jws-status';const results=document.createElement('div');results.id='jwsResults';results.className='jws-results';const boundary=document.querySelector('.search-boundary');if(boundary){boundary.innerHTML='<span>SEARCH EXECUTION</span><strong>Results stay inside JARVIS.</strong><small>Brave and Bing are queried through a read-only relay. Result links open only when you choose one.</small>';boundary.insertAdjacentElement('afterend',box);box.insertAdjacentElement('afterend',results);}}}
  window.addEventListener('click',e=>{const target=e.target instanceof Element?e.target.closest('#webSearch,[data-provider],[data-search]'):null;if(!target)return;const input=$('#webQuery'),provider=$('#webProvider');if(!input||!provider)return;let engine=provider.value;if(target.matches('[data-provider]'))engine=target.dataset.provider||engine;if(engine!=='brave'&&engine!=='bing')return;if(target.matches('[data-provider]'))provider.value=engine;e.preventDefault();e.stopImmediatePropagation();void search(input.value,engine);},true);
  window.addEventListener('keydown',e=>{if(e.key==='Enter'&&e.target?.matches?.('#webQuery')){e.preventDefault();e.stopImmediatePropagation();void search(e.target.value,$('#webProvider')?.value||'brave');}},true);
  new MutationObserver(mount).observe(document.documentElement,{childList:true,subtree:true});mount();
})();
