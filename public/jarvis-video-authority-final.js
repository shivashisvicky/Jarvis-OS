(()=>{
'use strict';
const FILTERS={all:'',videos:'&sp=EgIQAQ%3D%3D',shorts:'&sp=EgIQCQ%3D%3D'};
const PIPED=['https://pipedapi.tokhmi.xyz','https://pipedapi.moomoo.me','https://piped-api.garudalinux.org','https://piped-api.lunar.icu','https://api.piped.privacydev.net','https://pipedapi.smnz.de','https://pipedapi.adminforge.de','https://pipedapi.qdi.fi','https://piped-api.hostux.net','https://pdapi.vern.cc','https://pipedapi.rivo.lol','https://pipedapi.leptons.xyz'];
let mode='all',boundInput=null,boundButton=null,lastQuery='';
const $=s=>document.querySelector(s),$$=(s,r=document)=>[...r.querySelectorAll(s)];
const esc=s=>String(s??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
const valid=id=>/^[A-Za-z0-9_-]{11}$/.test(String(id||''));
const urlFor=(q,m=mode)=>`https://www.youtube.com/results?search_query=${encodeURIComponent(String(q||'').trim())}${FILTERS[m]||''}`;
async function json(url,ms=5000){const c=new AbortController(),t=setTimeout(()=>c.abort(),ms);try{const r=await fetch(url,{signal:c.signal,cache:'no-store',headers:{Accept:'application/json'}});if(!r.ok)throw Error(String(r.status));return await r.json()}finally{clearTimeout(t)}}
function norm(v,source){const id=String(v?.videoId||v?.id||'');if(!valid(id))return null;return{id,title:String(v?.title||'Untitled video'),author:String(v?.author||v?.uploader||v?.uploaderName||v?.channelName||'YouTube'),views:String(v?.viewCountText||v?.views||v?.viewCount||''),date:String(v?.publishedText||v?.uploadedDate||''),thumb:String(v?.videoThumbnails?.at?.(-1)?.url||v?.thumbnailUrl||v?.thumbnail||`https://i.ytimg.com/vi/${id}/hqdefault.jpg`),source,kind:/short/i.test(String(v?.type||''))?'short':'video'}}
function uniq(a){const m=new Map();for(const x of a||[])if(x?.id&&!m.has(x.id))m.set(x.id,x);return[...m.values()]}
function relevant(items,q){const terms=String(q).toLowerCase().split(/\s+/).filter(x=>x.length>1);return [...items].sort((a,b)=>terms.reduce((n,t)=>n+String(b.title+' '+b.author).toLowerCase().includes(t),0)-terms.reduce((n,t)=>n+String(a.title+' '+a.author).toLowerCase().includes(t),0)).filter(x=>terms.some(t=>String(x.title+' '+x.author).toLowerCase().includes(t)))}
async function piped(base,q){const d=await json(`${base}/search?q=${encodeURIComponent(q)}&filter=videos&region=IN&sort_by=relevance`);return(Array.isArray(d)?d:(d?.items||d?.videos||[])).map(x=>norm(x,'LIVE INDEX')).filter(Boolean)}
function status(s){$('#mediaState')?.replaceChildren(document.createTextNode(s));$('#jvcStatus')?.replaceChildren(document.createTextNode(s))}
function play(item){const id=typeof item==='string'?item:item?.id;if(!valid(id))return false;const p=$('#jarvisPlayer');if(!p)return false;p.innerHTML=`<iframe title="${esc(typeof item==='string'?'JARVIS video':item?.title||'JARVIS video')}" allow="autoplay; encrypted-media; picture-in-picture; fullscreen" allowfullscreen playsinline src="https://www.youtube-nocookie.com/embed/${encodeURIComponent(id)}?rel=0&playsinline=1&modestbranding=1"></iframe>`;status('PLAYING · JARVIS PLAYER');return true}
function ensureFilters(){const r=$('#videoResults');if(!r)return;let bar=$('#jvsFilters');if(!bar){bar=document.createElement('div');bar.id='jvsFilters';bar.className='jvs-filters';bar.innerHTML='<button type="button" data-jvs-mode="all">ALL</button><button type="button" data-jvs-mode="videos">VIDEOS</button><button type="button" data-jvs-mode="shorts">SHORTS</button>';r.parentElement?.insertBefore(bar,r)}$$('[data-jvs-mode]').forEach(b=>{b.classList.toggle('active',b.dataset.jvsMode===mode);if(!b.dataset.jvsBound){b.dataset.jvsBound='1';b.addEventListener('click',e=>{e.preventDefault();e.stopImmediatePropagation();mode=b.dataset.jvsMode||'all';void search(lastQuery)})}})}
function render(items,q){const r=$('#videoResults');if(!r)return;const shown=uniq(items).slice(0,12);r.innerHTML=shown.map(v=>`<button type="button" class="jvc-card jv4-video-card jvs-card" data-jvs-id="${esc(v.id)}"><img loading="lazy" src="${esc(v.thumb)}" alt=""><span class="video-meta"><strong>${esc(v.title)}</strong><small>${esc(v.author)}${v.views?` · ${esc(v.views)}`:''}</small><small>${esc(v.date)} · ${esc(v.source)}</small></span><b>▶</b></button>`).join('')||`<div class="empty"><strong>LIVE WEB SEARCH READY</strong><br>No API index returned verified results for “${esc(q)}”. Nothing canned or unrelated was substituted.<br><a class="secondary" href="${esc(urlFor(q))}" target="_blank" rel="noopener noreferrer">OPEN LIVE YOUTUBE RESULTS ↗</a></div>`;status(shown.length?`RESULTS · ${shown.length} · LIVE`:'LIVE WEB SEARCH READY');$$('.jvs-card',r).forEach(b=>b.onclick=e=>{e.preventDefault();e.stopImmediatePropagation();play(b.dataset.jvsId)})}
async function search(q){const query=String(q||'').trim()||'trending videos India';lastQuery=query;const input=$('#videoQuery');if(input)input.value=query;ensureFilters();status(`SEARCHING · LIVE SOURCES · ${query}`);const r=$('#videoResults');if(r)r.innerHTML='<div class="empty">JARVIS is racing live providers…</div>';const settled=await Promise.allSettled(PIPED.map(p=>piped(p,query)));const all=uniq(settled.flatMap(x=>x.status==='fulfilled'?x.value:[]));let items=relevant(all,query);if(mode==='shorts')items=items.filter(x=>x.kind==='short');if(mode==='videos')items=items.filter(x=>x.kind!=='short');if(items.length>=2){render(items,query);return}render([],query)}
function install(){const input=$('#videoQuery'),button=$('#videoSearch');if(input&&input!==boundInput){boundInput=input;input.addEventListener('keydown',e=>{if(e.key==='Enter'){e.preventDefault();e.stopImmediatePropagation();void search(input.value)}},true)}if(button&&button!==boundButton){boundButton=button;const n=button.cloneNode(true);button.replaceWith(n);boundButton=n;n.addEventListener('click',e=>{e.preventDefault();e.stopImmediatePropagation();void search(input?.value||'')},true)}if(input){window.jarvisVideoSearch=search;window.jarvisVideoSearchUrl=urlFor;window.jarvisVideoPlay=play;window.__JARVIS_VIDEO_AUTHORITY_FINAL__=true;ensureFilters()}}
install();new MutationObserver(install).observe(document.documentElement,{childList:true,subtree:true});
})();
