(()=>{'use strict';if(window.__JARVIS_EBOOK_COMPAT_V2__)return;window.__JARVIS_EBOOK_COMPAT_V2__=true;const API='https://gutendex.com/books/';const json=async(u)=>{const c=new AbortController(),t=setTimeout(()=>c.abort(),12000);try{const r=await fetch(u,{signal:c.signal,cache:'no-store'});if(!r.ok)throw Error('HTTP '+r.status);return r.json()}finally{clearTimeout(t)}};const text=b=>Object.keys(b?.formats||{}).some(k=>/^text\/(plain|html)/i.test(k)||/^application\/epub\+zip$/i.test(k));const sound=b=>/^(sound|audio)$/i.test(String(b?.media_type||b?.type||''));const cover=b=>b?.formats?.['image/jpeg']||b?.formats?.['image/png']||'';const norm=s=>String(s||'').toLowerCase().replace(/[^a-z0-9]+/g,' ').trim();let seq=0;const run=async()=>{const p=document.querySelector('#jbe6Panel'),box=p?.querySelector('#jbe6Results');if(!p||!box)return;const q=p.querySelector('#jbe6Query')?.value?.trim();if(!q)return;const my=++seq;try{const d=await json(`${API}?search=${encodeURIComponent(q)}&page=1`);if(my!==seq)return;const seen=new Set(),out=[];for(const b of d.results||[]){if(sound(b)||!text(b))continue;const k=norm(b.title)+'|'+norm((b.authors||[]).map(a=>a.name).join(','));if(seen.has(k))continue;seen.add(k);out.push(b)}box.innerHTML=out.map((b,i)=>`<article class="jbe6-book" data-book-id="${b.id}"><div class="jbe6-cover">${cover(b)?`<img loading="lazy" decoding="async" src="${cover(b)}" alt="" style="width:100%;height:100%;object-fit:cover;border-radius:5px">`:''}</div><div><div class="jbe6-name">${i+1}. ${String(b.title||'Untitled').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]))}</div><div class="jbe6-author">${(b.authors||[]).map(a=>a.name).join(', ')||'Unknown author'}</div><div class="jbe6-desc">${(b.subjects||[]).slice(0,3).join(' · ')||'Public-domain text'}</div></div><div class="jbe6-actions"><button type="button" class="jbe6-link primary" data-read="${b.id}" data-rel-read="${b.id}" data-title="${String(b.title||'').replace(/"/g,'&quot;')}">READ IN JARVIS</button><a class="jbe6-link" target="_blank" rel="noopener" href="https://www.gutenberg.org/ebooks/${b.id}">OPEN GUTENBERG</a></div></article>`).join('')||'<div class="jbe6-status">No readable text editions found.</div>';const s=p.querySelector('#jbe6StatusLine');if(s)s.textContent=out.length?`${out.length} RESULTS · GUTENBERG`:'NO READABLE EDITIONS';try{const cards=[...box.querySelectorAll('.jbe6-book')];const results=cards.map((c,i)=>({index:i,id:c.dataset.bookId,title:c.querySelector('.jbe6-name')?.textContent||'',author:c.querySelector('.jbe6-author')?.textContent||'',type:'BOOK'}));window.jarvisContextEngine?.set?.({domain:'BOOKS',entity:{type:'BOOK',title:results[0]?.title||''},query:q,results,selected:null},'merge')}catch{}}catch(e){if(my!==seq)return;const s=p.querySelector('#jbe6StatusLine');if(s)s.textContent='SEARCH ERROR';console.warn('[JARVIS ebook compat search]',e)}};window.jarvisEbookLibrarySearch=run;document.addEventListener('click',e=>{if(e.target?.closest?.('#jbe6Search')){e.preventDefault();e.stopImmediatePropagation();run()}},true);document.addEventListener('keydown',e=>{if(e.key==='Enter'&&e.target?.id==='jbe6Query'){e.preventDefault();e.stopImmediatePropagation();run()}},true);})();