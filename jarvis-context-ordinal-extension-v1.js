(()=>{
'use strict';
if(window.__JARVIS_CONTEXT_ORDINAL_EXTENSION_V1__)return;
window.__JARVIS_CONTEXT_ORDINAL_EXTENSION_V1__=true;
const clean=s=>String(s||'').replace(/\s+/g,' ').trim();
const isBookDomain=d=>['BOOKS','BOOK','BOOK_AUTHOR'].includes(String(d||'').toUpperCase());
const units={first:1,second:2,third:3,fourth:4,fifth:5,sixth:6,seventh:7,eighth:8,ninth:9,tenth:10,eleventh:11,twelfth:12,thirteenth:13,fourteenth:14,fifteenth:15,sixteenth:16,seventeenth:17,eighteenth:18,nineteenth:19};
const tens={twentieth:20,thirtieth:30,fortieth:40,fiftieth:50,sixtieth:60,seventieth:70,eightieth:80,ninetieth:90};
const ordinalValue=raw=>{let q=clean(raw).toLowerCase().replace(/[?.!]+$/,'').replace(/^the\s+/,'').replace(/\s+(?:one|result)$/,'');if(/^\d+$/.test(q))return Number(q);const m=q.match(/^(\d+)(?:st|nd|rd|th)$/);if(m)return Number(m[1]);if(units[q])return units[q];if(tens[q])return tens[q];const p=q.replace(/-/g,' ').split(/\s+/);if(p.length===2&&tens[p[1]]&&units[p[0]])return tens[p[1]]+units[p[0]];return null};
const isOrdinalCommand=q=>/^(?:please\s+)?(?:open|read|show)\s+(?:the\s+)?(?:\d+(?:st|nd|rd|th)?|[a-z]+(?:-[a-z]+)?)(?:\s+(?:one|result))?$/i.test(clean(q))&&ordinalValue(clean(q).replace(/^(?:please\s+)?(?:open|read|show)\s+/i,''))!==null;
const getContext=()=>{const live=window.jarvisContextEngine?.get?.();if(live?.active&&isBookDomain(live.domain)&&Array.isArray(live.results)&&live.results.length)return{ctx:live,source:'live'};try{const x=window.jarvisBookContextPersistence?.get?.()||JSON.parse(localStorage.getItem('JARVIS_LAST_BOOK_CONTEXT_V2')||'null')||JSON.parse(sessionStorage.getItem('JARVIS_LAST_BOOK_CONTEXT_V1')||'null');if(x&&Array.isArray(x.results)&&x.results.length&&Date.now()-Number(x.savedAt||0)<=30*60*1000)return{ctx:x,source:'last-book'}}catch{}const saved=window.jarvisContextMemory?.get?.();if(saved?.domain&&isBookDomain(saved.domain)&&Array.isArray(saved.results)&&saved.results.length)return{ctx:saved,source:'memory'};return null};
const explain=()=>{const t='I do not have a current book result list to read from. Search for a book first, then ask me to read a result.';const el=document.querySelector('#jarvisReply');if(el){el.textContent=t;el.classList.add('visible')}try{window.jarvisVoiceAuthoritySpeak?.(t)||window.jarvisCinematicSpeak?.(t)||window.jarvisSpeak?.(t)}catch{}};
const run=raw=>{const q=clean(raw);if(!isOrdinalCommand(q))return false;const target=clean(q.replace(/^(?:please\s+)?(?:open|read|show)\s+/i,''));const n=ordinalValue(target);if(!Number.isInteger(n)||n<1)return false;const hit=getContext();if(!hit){explain();return true}const item=hit.ctx.results[n-1];if(!item){const t=`I only have ${hit.ctx.results.length} book results in the current list.`;const el=document.querySelector('#jarvisReply');if(el){el.textContent=t;el.classList.add('visible')}try{window.jarvisVoiceAuthoritySpeak?.(t)||window.jarvisCinematicSpeak?.(t)||window.jarvisSpeak?.(t)}catch{}return true}const resolved={matched:true,type:'RESULT',index:n-1,value:item,domain:'BOOKS'};window.dispatchEvent(new CustomEvent('jarvis:context-followup',{detail:{type:'SELECT',text:target,context:hit.ctx,source:hit.source,resolved}}));return true};
const intercept=e=>{const q=clean(e.detail?.text);if(!run(q))return;e.preventDefault?.();e.stopImmediatePropagation?.()};
window.addEventListener('jarvis:voice-command',intercept,true);
document.addEventListener('submit',e=>{const f=e.target;if(!(f instanceof HTMLFormElement)||f.id!=='commandForm')return;const input=f.querySelector('#commandInput');const q=input instanceof HTMLInputElement?input.value:'';if(!run(q))return;e.preventDefault();e.stopImmediatePropagation();if(input instanceof HTMLInputElement)input.value=''},true);
window.jarvisContextOrdinalExtension={version:'1.0.0',run,ordinalValue};
})();