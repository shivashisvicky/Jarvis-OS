(() => {
  'use strict';
  // Intentionally inert. News must never make background AI requests.
  // Source snippets remain the lightweight informative layer.
  window.__JARVIS_NEWS_SUMMARIES__ = true;
})();
