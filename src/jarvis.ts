export type JarvisIntent='time'|'date'|'calculator'|'snake'|'notes'|'settings'|'files'|'fullscreen'|'status'|'help'|'search'|'maps'|'media'|'api'|'remote'|'web'|'joke'|'name'|'weather'|'unknown';
export type Telemetry={online:boolean;battery:number|null;charging:boolean|null;memory:number|null;cores:number|null;network:string};
export type JarvisResult={intent:JarvisIntent;reply:string;value?:string;url?:string};
export type VoiceConfig={voiceName:string;rate:number;pitch:number;volume:number;language:string};
const ql=(s:string)=>s.trim().toLowerCase();
export function classifyCommand(raw:string):JarvisIntent{const q=ql(raw);if(/\b(time|clock)\b/.test(q))return'time';if(/\b(date|day|today)\b/.test(q))return'date';if(/\b(joke|funny|make me laugh|laugh)\b/.test(q))return'joke';if(/\b(my name|who am i|what(?: is|['’]s|s) my name)\b/.test(q))return'name';if(/\b(weather|temperature|forecast|how hot|how cold)\b/.test(q))return'weather';if(/\b(calculator|calculate|math|compute)\b/.test(q))return'calculator';if(/\b(snake|game|arcade)\b/.test(q))return'snake';if(/\b(note|notes|remember)\b/.test(q))return'notes';if(/\b(setting|settings|appearance|theme|voice)\b/.test(q))return'settings';if(/\b(file|files|workspace)\b/.test(q))return'files';if(/\b(fullscreen|full screen|cinema)\b/.test(q))return'fullscreen';if(/\b(status|diagnostic|diagnostics|system check|health)\b/.test(q))return'status';if(/\b(help|commands|what can you do)\b/.test(q))return'help';if(/\b(news|headlines|current events|latest events)\b/.test(q))return'web';if(/\b(map|maps|directions|navigate|location)\b/.test(q))return'maps';if(/\b(video|videos|youtube|movie|movies|play|media)\b/.test(q))return'media';if(/\b(postman|api|rest|http request|request client)\b/.test(q))return'api';if(/\b(sftp|winscp|ssh|remote server|file transfer)\b/.test(q))return'remote';if(/\b(search|look up|google|bing|internet|web)\b/.test(q))return'search';return'unknown'}
const jokes=['Why did the developer go broke? Because they used up all their cache.','I told my computer I needed a break. It said, “I will go to sleep.”','There are only 10 kinds of people: those who understand binary and those who do not.'];
function weatherPlace(q:string){return q.replace(/\b(weather|temperature|forecast|what(?: is|['’]s|s)|how hot|how cold)\b/gi,'').replace(/^\s*(in|at|for|of)\s+/i,'').replace(/\s+/g,' ').trim()||'current location'}
export function runCommand(raw:string,telemetry:Telemetry):JarvisResult{const q=raw.trim(),l=q.toLowerCase(),intent=classifyCommand(q),now=new Date();switch(intent){case'time':return{intent,reply:`The local time is ${new Intl.DateTimeFormat([],{hour:'2-digit',minute:'2-digit',second:'2-digit'}).format(now)}.`};case'date':return{intent,reply:`Today is ${new Intl.DateTimeFormat([],{weekday:'long',month:'long',day:'numeric',year:'numeric'}).format(now)}`};case'joke':return{intent,reply:jokes[Math.floor(Math.random()*jokes.length)]};case'name':return{intent,reply:'You are Shivashis. JARVIS is at your service.'};case'weather':{const place=weatherPlace(q);window.dispatchEvent(new CustomEvent('jarvis:weather',{detail:{place}}));return{intent,reply:`Checking the weather for ${place}.`}}case'calculator':return{intent,reply:'Calculator module standing by.',value:'calculator'};case'snake':return{intent,reply:'Arcade module ready. Shall we play?',value:'snake'};case'notes':return{intent,reply:'Personal memory module ready.',value:'notes'};case'settings':return{intent,reply:'System preferences ready.',value:'settings'};case'files':return{intent,reply:'Local workspace is online.',value:'files'};case'fullscreen':return{intent,reply:'Engaging immersive display.'};case'status':return{intent,reply:`Core is online. ${telemetry.online?'Internet connection is available.':'I am in offline mode.'} WiFi/network: ${telemetry.network}. ${telemetry.cores??'Unknown'} logical processors detected.`};case'help':return{intent,reply:'Ask me naturally. I can tell jokes, remember your name, check weather, search the web, find videos, read news, open Maps, run REST requests, manage notes, and control the JARVIS workspace.'};case'maps':return{intent,reply:'Opening the mapping console.',value:'maps'};case'media':return{intent,reply:'Opening the media console.',value:'media'};case'api':return{intent,reply:'Opening the REST engineering lab.',value:'api'};case'remote':return{intent,reply:'Opening the secure transfer workspace.',value:'remote'};case'web':if(/\b(news|headlines|current events|latest events)\b/.test(l)){window.dispatchEvent(new CustomEvent('jarvis:news',{detail:{query:q}}));return{intent,reply:'Opening the live news desk.'}}return{intent,reply:`Opening web search for “${q.replace(/\b(search|look up|internet|web)\b/gi,'').trim()||'your query'}”.`,value:'web'};case'search':return{intent,reply:`Searching the web for “${q.replace(/\b(search|look up|internet|web)\b/gi,'').trim()||'your query'}”.`,value:'web'};default:return{intent,reply:'I did not recognise that as a module command, but I can still help. Try asking me a question, for example “tell me a joke”, “what is my name?”, or “weather in GGP Colony”.'}}}
export async function getTelemetry():Promise<Telemetry>{const nav=navigator as Navigator&{getBattery?:()=>Promise<{level:number;charging:boolean}>};let battery:number|null=null,charging:boolean|null=null;if(nav.getBattery){try{const b=await nav.getBattery();battery=Math.round(b.level*100);charging=b.charging}catch{}}const memory=(performance as Performance&{memory?:{usedJSHeapSize:number;jsHeapSizeLimit:number}}).memory;return{online:navigator.onLine,battery,charging,memory:memory?Math.round(memory.usedJSHeapSize/memory.jsHeapSizeLimit*100):null,cores:navigator.hardwareConcurrency??null,network:(navigator as Navigator&{connection?:{effectiveType?:string}}).connection?.effectiveType??'online'}}
export function availableVoices(){return'speechSynthesis'in window?window.speechSynthesis.getVoices():[]}
export function chooseJarvisVoice(){const voices=availableVoices(),preferred=['Daniel','Arthur','George','Oliver','James','Alex','Fred','Thomas'];return voices.find(v=>preferred.some(n=>v.name.toLowerCase().includes(n.toLowerCase()))&&/^en-GB/i.test(v.lang))??voices.find(v=>/^en-GB/i.test(v.lang)&&/male|natural|enhanced|premium/i.test(v.name))??voices.find(v=>/^en-GB/i.test(v.lang))??voices.find(v=>/^en-IN/i.test(v.lang)&&/male|natural|enhanced|premium/i.test(v.name))??voices[0]}
export function speak(text:string,cfg:VoiceConfig={voiceName:'',rate:.82,pitch:.52,volume:.98,language:'en-GB'}):void{if(!('speechSynthesis'in window))return;window.speechSynthesis.cancel();const u=new SpeechSynthesisUtterance(text);u.rate=cfg.rate;u.pitch=cfg.pitch;u.volume=cfg.volume;u.lang=cfg.language;const v=availableVoices().find(x=>x.name===cfg.voiceName)||chooseJarvisVoice();if(v){u.voice=v;u.lang=v.lang}window.speechSynthesis.speak(u)}
export function startVoice(onResult:(text:string)=>void,onState:(active:boolean,interim?:string)=>void,language='en-GB'):()=>void{const C=(window as Window&{SpeechRecognition?:new()=>SpeechRecognition;webkitSpeechRecognition?:new()=>SpeechRecognition}).SpeechRecognition??(window as Window&{webkitSpeechRecognition?:new()=>SpeechRecognition}).webkitSpeechRecognition;if(!C)throw new Error('Voice recognition is not supported by this browser.');const r=new C();r.lang=language;r.interimResults=true;r.continuous=false;r.maxAlternatives=3;r.onstart=()=>onState(true);r.onend=()=>onState(false);r.onerror=()=>onState(false);r.onresult=e=>{let interim='',final='';for(let i=e.resultIndex;i<e.results.length;i++){const t=e.results[i][0].transcript;if(e.results[i].isFinal)final+=t;else interim+=t}onState(true,interim);if(final.trim()){const ev=new CustomEvent('jarvis:voice-command',{detail:{text:final.trim()},cancelable:true});window.dispatchEvent(ev);if(ev.defaultPrevented)return;onResult(final.trim())}};r.start();return()=>{try{r.stop()}catch{}}}
declare global{interface SpeechRecognitionResultList{[index:number]:SpeechRecognitionResult}interface SpeechRecognitionEvent extends Event{resultIndex:number;results:SpeechRecognitionResultList}interface SpeechRecognition{lang:string;interimResults:boolean;continuous:boolean;maxAlternatives:number;onstart:(()=>void)|null;onend:(()=>void)|null;onerror:(()=>void)|null;onresult:((event:SpeechRecognitionEvent)=>void)|null;start():void;stop():void}}
