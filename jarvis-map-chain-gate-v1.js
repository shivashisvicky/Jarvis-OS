(()=>{'use strict';
// Retired: cross-surface context ownership is handled by the shared command/context pipeline.
// Intentionally inert. Kept as a compatibility file so the TEST shell can retain its cache-busted loader safely.
})();
