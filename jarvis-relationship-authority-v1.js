(()=>{
'use strict';
if(window.__JARVIS_RELATIONSHIP_AUTHORITY_V5__)return;
window.__JARVIS_RELATIONSHIP_AUTHORITY_V5__=true;
const clean=s=>String(s||'').replace(/\s+/g,' ').trim();
const isBooks=c=>{const d=String(c?.domain||'').toUpperCase();return c?.active&&['BOOKS','BOOK','BOOK_AUTHOR'].includes(d)&&Array.isArray(c.results)&&c.results.length};
const authorOf=r=>clean(r?.author||r?.authors||r?.creator||'');
const titleOf=r=>clean(r?.title||r?.name||'');
const speak=text=>{const el=document.querySelector('#jarvisReply');if(el){el.textContent=text;el.classList.add('visible')}try{window.jarvisVoiceAuthoritySpeak?.(text)||window.jarvisCinematicSpeak?.(text)||window.jarvisSpeak?.(text)}catch{}};
const currentBook=ctx=>{const s=ctx?.selected;if(s&&typeof s==='object'&&authorOf(s))return s;const title=clean(ctx?.entity?.title||'').toLowerCase();if(title){const hit=(ctx.results||[]).find(r=>titleOf(r).toLowerCase()===title);if(hit)return hit}return null};
const saveRelationship=(ctx,book,author)=>{const rel={kind:'BOOK_AUTHOR',author,title:titleOf(book),bookId:book?.id||null,contextTurn:Number(ctx?.turn||0),savedAt:Date.now()};window.__JARVIS_RELATIONSHIP_CONTEXT__=rel;window.__JARVIS_RELATIONSHIP_AUTHOR__=author;try{sessionStorage.setItem('JARVIS_RELATIONSHIP_CONTEXT_V1',JSON.stringify(rel))}catch{}};
const readRelationship=()=>{try{const live=window.__JARVIS_RELATIONSHIP_CONTEXT__;if(live?.kind==='BOOK_AUTHOR'&&Date.now()-Number(live.savedAt||0)<30*60*1000)return live;const saved=JSON.parse(sessionStorage.getItem('JARVIS_RELATIONSHIP_CONTEXT_V1')||'null');if(saved?.kind==='BOOK_AUTHOR'&&Date.now()-Number(saved.savedAt||0)<30*60*1000){window.__JARVIS_RELATIONSHIP_CONTEXT__=saved;return saved}}catch{}return null};
const clearRelationship=()=>{window.__JARVIS_RELATIONSHIP_CONTEXT__=null;window.__JARVIS_RELATIONSHIP_AUTHOR__='';try{sessionStorage.removeItem('JARVIS_RELATIONSHIP_CONTEXT_V1')}catch{}};
const whoWrote=/^(?:please\s+)?(?:who|which\s+person)\s+(?:wrote|authored|is\s+the\s+author\s+of)\s+(?:it|that|this)(?:\s+book)?[?!.]*$/i;
const otherBooks=/^(?:please\s+)?(?:show|find|give)\s+(?:me\s+)?(?:his|her|their)\s+(?:other|more)\s+books[?!.]*$/i;
const shorthand=/^(?:please\s+)?(?:show\s+me\s+)?(?:his|her|their)(?:\s+other\s+books)?[?!.]*$/i;
const run=raw=>{const q=clean(raw),isWho=whoWrote.test(q),isOther=otherBooks.test(q)||shorthand.test(q);if(!isWho&&!isOther)return false;const ctx=window.jarvisContextEngine?.get?.();if(!isBooks(ctx))return false;let rel=readRelationship();let book=currentBook(ctx);if(isWho){if(!book){speak('I have the book context, but I cannot determine its author from the current result.');return true}const author=authorOf(book)||clean(window.__JARVIS_RELATIONSHIP_AUTHOR__);if(!author){speak('I have the book context, but I cannot determine its author from the current result.');return true}saveRelationship(ctx,book,author);const title=titleOf(book);speak(title?`${title} was written by ${author}.`:`The author is ${author}.`);return true}if(!rel||!rel.author){speak('I need the author from the previous book question first. Ask me who wrote this, then ask for other books.');return true}if(Number(rel.contextTurn)&&Number(ctx.turn)&&Number(rel.contextTurn)!==Number(ctx.turn)){clearRelationship();speak('That author relationship is from an earlier book result. Ask me who wrote this again for the current book.');return true}const author=clean(rel.author),excludeId=rel.bookId||book?.id||null;const searchAuthor=window.jarvisEbookSearchAuthority?.searchAuthor;if(typeof searchAuthor!=='function'){speak(`I know the author is ${author}, but the author search is not ready.`);return true}speak(`Finding other books by ${author}.`);Promise.resolve(searchAuthor(author,excludeId)).then(count=>{if(!Number(count))speak(`I could not find other Gutenberg books by ${author}.`)}).catch(()=>speak(`I could not complete the book search for ${author}.`));return true};
const intercept=e=>{const raw=clean(e.detail?.text);if(!run(raw))return;e.preventDefault?.();e.stopImmediatePropagation?.()};
window.addEventListener('jarvis:voice-command',intercept,true);
document.addEventListener('submit',e=>{const f=e.target;if(!(f instanceof HTMLFormElement)||f.id!=='commandForm')return;const input=f.querySelector('#commandInput');const raw=input instanceof HTMLInputElement?input.value:'';if(!run(raw))return;e.preventDefault();e.stopImmediatePropagation();if(input instanceof HTMLInputElement)input.value=''},true);
window.jarvisRelationshipAuthority={version:'5.0.0',run};
})();
