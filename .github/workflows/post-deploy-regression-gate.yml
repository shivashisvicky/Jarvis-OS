name: Post-Deploy Regression Gate

on:
  workflow_run:
    workflows: ["Deploy Jarvis OS"]
    types: [completed]
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read
  actions: read

concurrency:
  group: jarvis-post-deploy-regression-gate
  cancel-in-progress: true

env:
  PNPM_VERSION: 10.15.0

jobs:
  sequential-command-gate:
    name: Sequential / Beowulf -> Newman -> Time -> Context
    # A cancelled/failed deployment is not a product regression. Do not turn
    # the post-deploy gate red or run browser tests against an unconfirmed
    # production version. The next successful deployment will trigger a new
    # gate automatically. Manual workflow_dispatch remains available for
    # deliberate diagnostics.
    if: github.event_name != 'workflow_run' || github.event.workflow_run.conclusion == 'success'
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - name: Verify production Pages job succeeded
        if: github.event_name == 'workflow_run'
        env:
          GH_TOKEN: ${{ github.token }}
          UPSTREAM_RUN_ID: ${{ github.event.workflow_run.id }}
        run: |
          set -euo pipefail
          echo "UPSTREAM_RUN_ID=$UPSTREAM_RUN_ID"
          curl --fail --silent --show-error \
            -H "Authorization: Bearer $GH_TOKEN" \
            -H 'Accept: application/vnd.github+json' \
            -H 'X-GitHub-Api-Version: 2026-03-10' \
            "https://api.github.com/repos/${{ github.repository }}/actions/runs/$UPSTREAM_RUN_ID/jobs?per_page=100" > jobs.json
          python - <<'PY'
          import json
          data=json.load(open('jobs.json'))
          jobs=data.get('jobs',[])
          target=next((j for j in jobs if j.get('name')=='Deploy production Pages'),None)
          if not target:
              raise SystemExit('DEPLOY_PRODUCTION_JOB_NOT_FOUND')
          print('DEPLOY_PRODUCTION_JOB_STATUS=',target.get('status'))
          print('DEPLOY_PRODUCTION_JOB_CONCLUSION=',target.get('conclusion'))
          if target.get('conclusion') != 'success':
              raise SystemExit('DEPLOY_PRODUCTION_JOB_NOT_SUCCESS')
          PY

      - name: Checkout deployed commit
        uses: actions/checkout@v4
        with:
          ref: ${{ github.event.workflow_run.head_sha || github.sha }}

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 22

      - name: Install pnpm
        run: npm install --global pnpm@${PNPM_VERSION} && pnpm --version

      - name: Install dependencies
        run: pnpm install --no-frozen-lockfile

      - name: Install Chromium
        run: pnpm exec playwright install --with-deps chromium

      - name: Verify deployed shell in strict sequence
        env:
          JARVIS_LIVE_URL: https://shivashisvicky.github.io/Jarvis-OS/?deploy=${{ github.event.workflow_run.head_sha || github.sha }}
        run: |
          set -euo pipefail
          echo "DEPLOYED_COMMIT=${JARVIS_LIVE_URL##*=}"
          pnpm exec playwright test tests/e2e/post-deploy-regression-gate.spec.ts --project=chromium --workers=1 --reporter=line

      - name: Upload regression evidence
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: post-deploy-regression-evidence-${{ github.event.workflow_run.head_sha || github.sha }}
          path: |
            playwright-report/
            test-results/
          if-no-files-found: ignore
          retention-days: 21
