name: Jarvis OS CI
on:
  push:
    branches: [main, 'build/**', 'fix/**']
  pull_request:
    branches: [main]
  schedule:
    - cron: '17 * * * *'
concurrency:
  group: jarvis-ci-${{ github.ref }}
  cancel-in-progress: true
jobs:
  verify:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
      - run: npm install
      - run: npm run build
      # GitHub-hosted Ubuntu runners already provide the system browser dependencies.
      # Avoid Playwright's apt dependency bootstrap here because transient Ubuntu mirror
      # failures can cancel the entire verification job before the browser suite starts.
      - run: npx playwright install chromium
      - run: npm run test:browser
